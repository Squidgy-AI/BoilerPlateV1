/**
 * HeyGen Service - REMOVED
 * Avatar now uses SDK speak method directly in InteractiveAvatar component
 * This service is no longer needed since HeyGen streaming was disabled
 */

// 📝 Note: HeyGen functionality has been moved to direct SDK usage
// Avatar speech now handled by InteractiveAvatar.tsx using localAvatarRef.current.speak()

console.log('📝 HeyGen service loaded - functions moved to direct SDK usage');

// Export empty object to prevent import errors
export default {};
